---
import { getStaticLocalePaths } from '@src/i18n/utils';

export function getStaticPaths() {
  return getStaticLocalePaths();
}

const { locale } = Astro.params;
return Astro.redirect(`/${locale}/professional-dot-plot-tool/`, 301);
---
